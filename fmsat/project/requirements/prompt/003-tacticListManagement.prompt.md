# Source prompt — requirement 003

> We will need to add a tactic list screen. We can use the screenshot as a
> thumbnail for it. We should also have a checkmark so we can delete all those
> checked, etc. Add this as an additional todo.
