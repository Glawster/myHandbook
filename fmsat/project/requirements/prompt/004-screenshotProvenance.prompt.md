# Source prompt — requirement 004

> If I select a player in the squad list I would like to right-click to Show
> Screenshot where this data came from. The user can use that to correct any
> mistakes. Show Screenshot pops up a new window, not overlapping the current
> window, and has a Close button. At some point we will need a remove-screenshot
> selection window to remove old data in the database.
